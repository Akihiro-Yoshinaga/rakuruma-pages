# rakuruma-pages（自社サイト）作業ルール

## 口コミの掲載ルール（2026-09-08 確定・変更するときは handbook `05_sales/口コミ素材_2026-08-05.md` と両方直す）

### くらしのマーケットの口コミ本文
- 転載は利用規約で**原則禁止**。出店者FAQの暫定措置で**サイト全体で3件まで**だけ許される。4件目は載せない。
- 載せる3件は「原文のまま」。言い換え・要約・誤字修正もしない。短くするときは文単位で抜き、`（一部抜粋）`／`（中略）`を明記する。
- 各カードに必ず `引用元：くらしのマーケット` と**該当口コミが載っているページのURL**（サービスの `/review/` ページ）を付ける。
- 現在の3件：ドラレコ外国車 2026-07-04（施工修正）／デジミラ 2026-06-29（リピーター）／ドラレコ国産 2026-06-08（できる事とできない事）。差し替えるときも合計3件を超えない。
- Googleクチコミは `（Google）` を出典として明記する。出典が分からない口コミは載せない。
- 注記は「編集のうえ」と書かない（原文掲載が前提）。

### 件数・平均評価の数字
- HTMLに数字を直書きしない。必ず `<span data-curama-count>596</span>` ／ `<span data-curama-rating>4.92</span>` の形にして、ページ末尾で `<script src="/scripts/stats.js" defer></script>` を読み込む（GASの `action=curamaStats` が店舗全体の最新値で書き換える）。
- 予備値（JSが失敗したときに見える値）は**店舗全体**の数字（現在 4.92／596件）に揃える。個別サービスの評価（4.93など）を混ぜない。
- 洗車の「口コミ◯件すべて★5」のような**サービス別の主張**も直書きしない。`<span data-curama-wash-count>30</span>`／`<span data-curama-wash-claim>すべて★5</span>`／`<span data-curama-wash-avg>5.0</span>` を使う（同じAPIが `washCount/washFive/washAvg` を返し、★5でない口コミが入れば表示が「★5が◯件」に自動で変わる）。
- Googleの口コミ件数・評価も同じAPIが `googleCount/googleRating`（GBP APIから6時間ごと）で返す。HTMLは `<span data-google-stat hidden>／Google 口コミ<span data-google-count></span>件 ★<span data-google-rating></span></span>` の形（数字は書かない。値が来たときだけ表示される）。
- 「No.1」「最安」「地域一」などの最上級表現は書かない（くらマ出店者ガイドライン・景表法。根拠の明示が必要）。
- meta description・OGP など JS で書き換えられない場所には数字を書かない。
- 「○年○月現在」の固定日付つきの主張（例：エリア内24件すべて★5）は新規に書かない。書くなら検証できる現在の事実に限り、月次で見直す前提を注記する。
- 印刷用・PDF用の資料だけは日付入りの静的な数字でよい（「最新は /press/ 参照」を添える）。

### 法令の観点
- 自社サイトに載せた口コミは「事業者の表示」。改変・都合のよい切り取りは景表法（優良誤認）の対象になる。原文・出典・時期を守る。
- 口コミ本文の著作権はお客様にある。引用の形式（出典明記・原文・主従関係）を外さない。

## 同時編集の注意
- このフォルダは Google Drive 同期で、複数セッションが同時に触ることがある。コミット前に `git status` で自分の変更以外が混ざっていないか確認し、混ざっていたら分けてコミットする。

## ホバーの持ち上げ基準（2026-09-23 確定・全公開ページ共通）
- 原則「動く＝押せる」。押して何かが起きる要素だけ持ち上げ、情報カード（サービス紹介・お悩み・お客様の声・担当者など）はホバーで動かさない。
- 持ち上げ量：ボタン（LINE・予約・相談・見る系）は `translateY(-2px)`、カード型リンク（実績カード・LPカード・あわせてチェック）は `translateY(-3px)`、右下のフローティングLINEは `scale(1.05)`。4px・5px・8px は使わない。
- 持ち上げ・影の変化は必ず `@media (hover: hover) { ... }` の中に書く（タッチ端末でタップ後に浮いたまま残るのを防ぐ）。色・背景だけの変化は外でもよい。
- 実績カードは `.work-card.is-link:hover` にだけ書く（リンクなしの `.work-card` は動かさない）。
- `scripts/reveal.js` は現れ終わったら `.rv-fade`/`.is-in` を外す実装になっている。外さないと `.rv-fade.is-in{transform:translateY(0)}` が同じ詳細度・後読みで `:hover` の持ち上げを打ち消し、「画面の下にあったカードだけ浮かない」不揃いが再発する。
